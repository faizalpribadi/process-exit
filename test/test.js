'use strict';
var processExit = require('../');

describe('Should be test of method on process-exit', function() {
	it('has own property if available in the method', function(next) {
		// do something
	});
});
